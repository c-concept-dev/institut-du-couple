name: Générer Index des Modules

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  generate-indexes:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v3
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.x'
    
    - name: Generate module indexes
      run: python generate_module_indexes.py
      
    - name: Commit and push if changed
      run: |
        git config --global user.name 'GitHub Actions Bot'
        git config --global user.email 'actions@github.com'
        git add .
        git diff --quiet && git diff --staged --quiet || (git commit -m "🤖 Mise à jour automatique des index de modules [skip ci]" && git push)
